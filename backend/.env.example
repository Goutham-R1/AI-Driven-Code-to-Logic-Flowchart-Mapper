# Choose: "mock", "openai", or "gemini"
AI_PROVIDER=gemini

# Only needed if AI_PROVIDER=openai
OPENAI_API_KEY=your_openai_key_here

# Only needed if AI_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_api_key_here

# Optional
PORT=8000
